{
  "name": "website-updater",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension plus optional webhook bridge for live website update alerts.",
  "scripts": {
    "bridge": "node bridge-server.js",
    "check": "node --check background.js && node --check content.js && node --check popup.js && node --check options.js && node --check bridge-server.js"
  }
}
